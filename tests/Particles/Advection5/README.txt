DEFAULT SETUP:
64 particles on 1x10 qubic grids (distributed among 3 procs by default (see run.sh)). Uniform and steady flow. Particle(s) pass surface faces and are exchanged between processes. Connect (CON) and periodic (PER) boundaries are passed.
