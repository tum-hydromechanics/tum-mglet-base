DEFAULT SETUP:
8 particles on 1x2 qubic grids (distributed among 2 procs by default (see run.sh)). Uniform and steady flow. Particle(s) pass surface faces and are exchanged between processes.
Connect (CON) and periodic (PER) boundaries are passed. GRID ENLARGEMENT IS PROVOKED!
