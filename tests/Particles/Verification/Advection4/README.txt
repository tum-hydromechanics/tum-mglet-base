DEFAULT SETUP:
One particle on 2x2x2 qubic grids (distributed among 8 procs by default (see run.sh)). Uniform and steady flow. Particle passes different types of faces and is (potentially) exchanged between processes. Connect (CON) and periodic (PER) boundaries are passed.
