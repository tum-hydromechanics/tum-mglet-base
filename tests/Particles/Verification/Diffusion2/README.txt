DEFAULT SETUP:
10000 particles on 2x2x2 grids, starting as a point source at the point where all grids touch.
No flow, homogeneous and isotropic diffusion (Dx /= 0, Dy=Dz=0). Particles are (only) moving along the edges of grids.
